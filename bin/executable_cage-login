#!/bin/bash
set -e

container volume create claude-creds 2>/dev/null || true

exec container run -it --rm \
  --volume "claude-creds:/home/claude/.claude" \
  --env TERM \
  claude-isolated \
  claude login
