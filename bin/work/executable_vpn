#! /usr/bin/env bash

# vpnName=${1:-Winnipegvpn.iqmetrix.com}
vpnName=${1:-Reginavpn.iqmetrix.com}

pwd="$(lp-getpwd adfs.iqmetrix.com)"
if [[ -n $pwd ]]; then
	echo -e "0\n\n$pwd\n" | /opt/cisco/anyconnect/bin/vpn -s connect "$vpnName"
else
	echo Could not retreive credentials
fi
