fuzzy-dir() {
    if [[ -d "$1" ]]; then
        local dest=$( \
          cd "$1" && \
          echo $(fd --type d --max-depth 1 '' | fzf --no-select-1 --query "$2")
        )
        cd "$1/$dest"
    else
        echo "$1" is not a directory
    fi
}
fuzzy-dir "$@"
